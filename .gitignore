node_modules
.next
out
.DS_Store
*.tsbuildinfo
next-env.d.ts
.env*.local
.vercel
.env*
# Names only, no values. The .env* rule above would otherwise hide it.
!.env.example
/data
*.db

# clerk configuration (can include secrets)
/.clerk/

# Clerk keyless-mode scratch keys
.clerk
